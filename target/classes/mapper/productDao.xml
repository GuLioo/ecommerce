<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ecommerce.dao.productDao">
    <!--为DAO接口方法提供SQL语句配置-->
    <insert id="insert">
        insert ignore into product(pname,market_Price,image,pdesc,pnum)
        values (#{pname},#{market_Price},#{image},#{pdesc},#{pnum})
    </insert>
    <delete id="deleteByPrimaryKey"  >
    delete from product
    where pid = #{pid,jdbcType=INTEGER}
  </delete>
    <update id="updateByPrimaryKey">
        update product
        set pname =#{pname},market_Price=#{market_Price},image=#{image},pdesc=#{pdesc},pnum=#{pnum}
        where pid=#{pid};
    </update>
    <select id="selectByPrimaryKey" parameterType="Integer" resultType="product">
      select *
      from product
      where pid=#{pid}
    </select>
    <select id="selectAll" resultType="product">
        select * from product limit #{offset},#{limit}
    </select>
    <select id="countProducyAll" resultType="Integer">
        select count(*) from product
    </select>
    <select id="countProducyByname" parameterType="String" resultType="Integer">
        select count(*) from product where pname=#{pname}
    </select>
</mapper>